import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <nav className="header">
            <h1>Selector Master</h1>
        </nav>
    );
};

export default Header;